# Forklift design

**The canonical design document is [`docs/DESIGN.html`](DESIGN.html)** — vision, principles,
local and serverless architecture, the phased roadmap with per-feature status, idea reviews,
and the progress changelog. Open it in a browser.

This file exists only as a pointer so that markdown-first readers find their way there.
When a feature lands, update the status chips and changelog in `DESIGN.html`.
